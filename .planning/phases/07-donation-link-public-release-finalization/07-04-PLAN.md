---
phase: 07-hevy-import-ux-donation-link
plan: "04"
type: execute
wave: 3
depends_on: ["07-02", "07-03"]
files_modified:
  - .planning/ROADMAP.md
autonomous: false
requirements:
  - D-09

must_haves:
  truths:
    - "ROADMAP.md Phase 7 goal is updated to reflect the implemented scope"
    - "Human visually confirms cache banner, API section, and Settings additions work in browser"
  artifacts:
    - path: ".planning/ROADMAP.md"
      provides: "Updated Phase 7 entry"
      contains: "Hevy Import UX + Donation Link"
  key_links:
    - from: ".planning/ROADMAP.md Phase 7 goal"
      to: "CONTEXT.md D-09 decision"
      via: "goal text matches: 'Hevy Import UX + Donation Link'"
      pattern: "Hevy Import UX"
---

<objective>
Update ROADMAP.md Phase 7 goal text (D-09) and run a human visual validation checkpoint to confirm all Phase 7 UI is working correctly in the browser before marking the phase complete.

Purpose: Completes the D-09 documentation decision and provides a human gate to catch UI issues before the phase is closed. The ROADMAP update also reflects the plan list that was actually executed.
Output: ROADMAP.md updated; human confirms UI works end-to-end.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/workspace/GarminHevyMerge/.planning/PROJECT.md
@/workspace/GarminHevyMerge/.planning/ROADMAP.md
@/workspace/GarminHevyMerge/.planning/STATE.md
@/workspace/GarminHevyMerge/.planning/phases/07-donation-link-public-release-finalization/07-02-SUMMARY.md
@/workspace/GarminHevyMerge/.planning/phases/07-donation-link-public-release-finalization/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ROADMAP.md Phase 7 entry (D-09)</name>
  <files>.planning/ROADMAP.md</files>

  <read_first>
    - .planning/ROADMAP.md — read ENTIRE file; locate Phase 7 section
    - .planning/phases/07-donation-link-public-release-finalization/07-CONTEXT.md — D-09 decision and phase boundary description
  </read_first>

  <action>
Find the Phase 7 section in `.planning/ROADMAP.md` and update it to accurately reflect what was built.

Current Phase 7 entry in ROADMAP.md:
```markdown
### Phase 7: Hevy Import UX + Donation Link

**Goal:** Persistent Hevy CSV cache (no re-upload every session), optional Hevy API beta integration (fetch workouts directly with an API key), and a donation link in Settings.
**Requirements**: D-01 through D-09 (see 07-CONTEXT.md)
**Depends on:** Phase 6
**Plans:** 0 plans

Plans:
- [ ] TBD (run /gsd-plan-phase 7 to break down)
```

Replace with:
```markdown
### Phase 7: Hevy Import UX + Donation Link

**Goal:** Persistent Hevy CSV cache (no re-upload every session), optional Hevy API beta integration (fetch workouts directly with an API key), and a donation link in Settings.
**Requirements**: D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09
**Depends on:** Phase 6
**Plans:** 5 plans

Plans:
- [ ] 07-00-PLAN.md — Wave 0: RED test stubs for all new Phase 7 behaviors
- [ ] 07-01-PLAN.md — Wave 1: Backend — cache-status, use-cache, hevy/test, hevy/workouts routes + parse_hevy_api_response + DONATION_URL + /api/config
- [ ] 07-02-PLAN.md — Wave 2a: Upload screen — cache banner state machine + Hevy API section
- [ ] 07-03-PLAN.md — Wave 2b: Settings screen — HEVY API (BETA) group + CSV cache warning + donation footer
- [ ] 07-04-PLAN.md — Wave 3: ROADMAP update (D-09) + human visual checkpoint
```

No other sections of ROADMAP.md should be modified.
  </action>

  <verify>
    <automated>grep -A 12 "Phase 7: Hevy Import UX" .planning/ROADMAP.md</automated>
  </verify>

  <acceptance_criteria>
    - .planning/ROADMAP.md contains `07-00-PLAN.md`
    - .planning/ROADMAP.md contains `07-01-PLAN.md`
    - .planning/ROADMAP.md contains `07-02-PLAN.md`
    - .planning/ROADMAP.md contains `07-03-PLAN.md`
    - .planning/ROADMAP.md contains `07-04-PLAN.md`
    - .planning/ROADMAP.md contains `**Plans:** 5 plans` under Phase 7
    - .planning/ROADMAP.md contains `D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09` under Phase 7 Requirements
    - grep "TBD (run /gsd-plan-phase 7" .planning/ROADMAP.md returns empty (placeholder removed)
  </acceptance_criteria>

  <done>ROADMAP.md Phase 7 entry updated with all 5 plans and D-09 requirements list</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Wave 2a added a cache banner and Hevy API section to the Upload screen. Wave 2b added the HEVY API (BETA) settings group, CSV CACHE WARNING field, and a donation footer to the Settings screen. Wave 1 added 4 new backend routes and extended /api/upload to persist CSV caches.
  </what-built>

  <how-to-verify>
    **Setup:**
    1. Restart the app server (required — new routes are live, template cache cleared):
       ```bash
       pkill -f "python app.py"; sleep 1; cd /workspace/GarminHevyMerge && .venv/bin/python app.py &
       ```
    2. Open http://localhost:5000 in your browser

    **Test A — Upload screen (no cache):**
    3. Navigate to Upload screen (first screen)
    4. Confirm: the Hevy section shows ONLY the CSV drop zone + an "OR CONNECT VIA API" section below it — NO cache banner appears (no data/hevy_cache.csv exists yet)

    **Test B — Upload a CSV to create cache:**
    5. Upload a Garmin FIT file + Hevy CSV + select timezone → click Continue
    6. Confirm: upload succeeds as normal

    **Test C — Upload screen with cache:**
    7. Navigate back to Upload (click logo or back button)
    8. Confirm: a cache banner now appears ABOVE the CSV drop zone showing workout count + date (e.g. "95 workouts · Apr 22")
    9. If more than 7 days old: confirm an OUTDATED chip appears in amber
    10. Click "Use cached export" → confirm banner transitions to show chip good "CACHED" + "Upload new instead" link; CSV drop zone disappears
    11. Select a FIT file + timezone → confirm Continue button enables (no CSV upload needed)
    12. Click "Upload new instead" → confirm CSV drop zone reappears; CACHED state clears

    **Test D — Settings screen:**
    13. Click Settings tab in the top nav
    14. Confirm the card order: TIMEZONE DEFAULT → EXPORT FILENAME → OUTPUT FOLDER → HEVY API [BETA chip] → DANGER ZONE → "Support this project" link at the very bottom
    15. In the OUTPUT FOLDER card: confirm a "CSV CACHE WARNING" section appears below the folder path input, with a number field defaulting to 7 and a "days" label
    16. In the HEVY API card: confirm the disclaimer "Hevy's API is unofficial and may change without notice." appears; confirm the API key text input with placeholder "Paste your API key here…"
    17. Leave the API key field empty → confirm "Test connection" button is disabled
    18. Type any text in the API key field → confirm "Test connection" button enables
    19. Click "Test connection" → confirm a chip appears: either "INVALID KEY" or "UNREACHABLE" (acceptable — no valid key available)
    20. Confirm "Support this project" link at the bottom; clicking it should open a new tab (href="#" is fine for now)

    **Test E — Full test suite:**
    21. Run: `.venv/bin/pytest tests/ -v`
    22. Confirm: all tests PASS (expected: 72 total — 67 original + 5 new Phase 7 tests)
  </how-to-verify>

  <resume-signal>Type "approved" if all checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human → browser | Visual verification step; no new code deployed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-11 | Tampering | ROADMAP.md manual edit | accept | ROADMAP.md is a planning document in git; accidental change is recoverable via git diff/revert |
</threat_model>

<verification>
Final automated verification after human checkpoint passes:

```bash
# Full test suite
.venv/bin/pytest tests/ -v

# Verify ROADMAP has all plan entries
grep "07-0[0-4]-PLAN.md" .planning/ROADMAP.md | wc -l  # should be 5

# Verify donation URL constant in app.py
grep "DONATION_URL" app.py  # should show 2 lines: const definition + route

# Verify cache path constant
grep "CACHE_PATH" app.py  # should show multiple lines

# Verify new routes are registered
grep "@app.route.*hevy" app.py  # should show 4 routes
```
</verification>

<success_criteria>
- ROADMAP.md Phase 7 updated with 5 plan entries and D-01 through D-09 requirements
- Human has confirmed: cache banner appears on Upload screen after first upload; cache flows work (use + dismiss); Settings screen has HEVY API card, CSV cache warning, donation link in correct order
- Full test suite GREEN (72 tests)
- Phase 7 is ready for /gsd-verify-work
</success_criteria>

<output>
After completion, create `.planning/phases/07-hevy-import-ux-donation-link/07-04-SUMMARY.md`
</output>
